import 'dart:ui';

const apptitle = 'Expanse Tracker';
const rupee = '₹';
const lavenderBlush = Color(0XFFFEF0F5);
const englishViolet = Color(0XFF563C5C);
fSize(size, fontsize) =>
    fontsize * ((size.height / 100 * size.width / 100) / 100) * 3;

width(size, width) => width * (size.width / 1000);
height(size, height) => height * (size.height / 1000);
